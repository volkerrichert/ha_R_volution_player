meta {
  name: Movies MenuRequest1-2Menu_Get
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/Menu
  body: json
  auth: inherit
}

params:query {
  ~AuthKey: {{authKey}}
  ~Collection: {{firstCollectionId}}
  ~ByName: Movies
  ~Type: Menu
  ~IsParentalControlActive: false
  ~ApiKey: {{apiKey}}
  ~Start: 0
  ~Take: 3
  ~ById: 
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "AuthKey": "{{authKey}}",
    "Collection": "{{firstCollectionId}}",
    "ByName": "Movies",
    "Type": "Menu",
    "IsParentalControlActive": false,
    "ApiKey": "{{apiKey}}"
  }
}
